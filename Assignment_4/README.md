To run code, open Jupyter Notebook and download the .ipynb file. 
Then starting at the very top cell( each section is its own cell),
Click run at the top and run selected cell. You could also do a run all cells.
Go through each cell and run them all.

